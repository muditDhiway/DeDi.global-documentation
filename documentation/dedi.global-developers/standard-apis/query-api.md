# Query API

The **Query API** retrieves all child nodes of a hierarchy.

#### Endpoints

```
GET /query/namespace
GET /query/namespace/registry
```

* `/query/namespace` → Lists registries under a namespace.
* `/query/namespace/registry` → Lists records under a registry.

#### Returns

* Registry information
* Array of child nodes of a hierarchy
